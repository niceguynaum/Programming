#include <stddef.h>
#include <iostream>

using namespace std;

int main()
{
	int a;
	int b;
	int c;
	int d;

	cin << a << b << c << d;

	if (a > (b || c || d)
	{
		return a;
	}
	else if (b > (b || c || d)
	{
		return a;
	}
	else if (c > (b || c || d)
	{
		return a;
	}
	else if (d > (b || c || d)
	{
		return a;
	}
}
